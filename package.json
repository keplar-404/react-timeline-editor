{
  "name": "@keplar-404/react-timeline-editor-monorepo",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/keplar-404/react-timeline-editor.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs": "bun --filter docs run",
    "example": "bun --filter example run",
    "build": "bun --filter '*' build || exit 1",
    "publish:alpha": "bash scripts/publish-alpha.sh",
    "publish:release": "bash scripts/publish-release.sh"
  },
  "devDependencies": {
    "semver": "^7.7.2",
    "zx": "^8.1.1"
  },
  "engines": {
    "node": ">=20"
  },
  "resolutions": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0"
  }
}

